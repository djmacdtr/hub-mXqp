# LLM Smart Cache 测试逻辑

## 1. Redis 连接测试

```powershell
docker ps --filter "name=week17-redis-cache"
```

预期：Redis 容器正在运行。

## 2. 静态检查

```powershell
D:\AI_study_env\miniconda3\envs\py312\python.exe -m py_compile llm_cache\*.py demo_smart_cache.py
```

预期：无语法错误。

## 3. demo 测试

```powershell
D:\AI_study_env\miniconda3\envs\py312\python.exe demo_smart_cache.py
```

预期：

- Embedding 缓存能写入和命中。
- SemanticCache 能用相似问题命中回答。
- SemanticMessageHistory 能保存历史并检索相关消息。
- SemanticRouter 能把用户问题路由到对应 target。

## 4. 单元测试

```powershell
D:\AI_study_env\miniconda3\envs\py312\python.exe -m unittest discover -s tests
```

预期：所有测试通过。

## 5. 重点测试点

- 相同文本 embedding 不重复计算。
- 相似 prompt 命中语义缓存。
- 不相关 prompt 不命中语义缓存。
- 不同 session 的消息历史互不影响。
- 问候、退款、技术支持能路由到不同 target。
